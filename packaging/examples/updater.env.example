# Payram Updater Configuration
# Copy this file to /etc/payram/updater.env and customize the values

# HTTP server port (default: 2359)
# The server binds to 127.0.0.1 only for security
UPDATER_PORT=2359

# Policy URL - JSON file containing update policy
# Required: Must return policy with latest, releases, and breakpoints
POLICY_URL=https://raw.githubusercontent.com/payram/policy/main/update-policy.json

# Runtime manifest URL - JSON file containing container configuration
# Required: Must return manifest with image, defaults, and overrides
RUNTIME_MANIFEST_URL=https://raw.githubusercontent.com/payram/manifests/main/runtime-manifest.json

# Fetch timeout in seconds (default: 10)
# Timeout for HTTP requests to policy and manifest URLs
FETCH_TIMEOUT_SECONDS=10

# State directory - where job state is persisted
# Default: /var/lib/payram
STATE_DIR=/var/lib/payram

# Log directory - where upgrade logs are written
# Default: /var/log/payram
LOG_DIR=/var/log/payram
